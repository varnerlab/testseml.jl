language: julia

julia:
  - 1.1

os:
  - linux

notifications:
  email: false
before_install:
  - /usr/bin/python3 -m pip install python-libsbml --user 
jobs:
  include:
    - stage: "Documentation"
      julia: 1.1
      os: linux
      script:
        - julia --project -e 'using Pkg; Pkg.build(; verbose = true); Pkg.test(coverage=true)'
      after_success: skip
